package abstraction;

public class MainClass {
    public static void main(String[] args) {
        // Vehicle vec = new Vehicle(6); // here we get error as Vehicle is an abstrsct
        // class so we can't create its
        // object
        Car car = new Car();
    }
}
